@page "/"
@inject NavigationManager navman

<RadzenButton Icon="add"  ButtonStyle="ButtonStyle.Primary" Click="x => ApriScheda()"></RadzenButton>
<RadzenRow>
    <RadzenColumn>
        <RadzenCard>
            <RadzenText>Nome</RadzenText>
            <RadzenText>Gioco</RadzenText>
        </RadzenCard>
    </RadzenColumn>
    <RadzenColumn>
        <RadzenCard>
            <RadzenText>Nome</RadzenText>
            <RadzenText>Gioco</RadzenText>
        </RadzenCard>
    </RadzenColumn>
</RadzenRow>


@code {
    List<Personaggio> Personaggi = new List<Personaggio>(); 

    private void ApriScheda(int personaggioId = 0)
    {
        navman.NavigateTo($"/scheda/{personaggioId}"); 
    }
}
